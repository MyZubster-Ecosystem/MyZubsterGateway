const express=require("express");const{Role,hasPermission,PERMISSIONS,seedRoles}=require("./rbac");const router=express.Router();router.post("/setup",hasPermission(PERMISSIONS.ADMIN_MANAGE_ROLES),async(req,res)=>{try{await seedRoles();res.json({message:"Roles seeded"})}catch(err){res.status(500).json({error:err.message})}});router.get("/",hasPermission(PERMISSIONS.ADMIN_MANAGE_ROLES),async(req,res)=>{try{const roles=await Role.find();res.json(roles)}catch(err){res.status(500).json({error:err.message})}});router.get("/:name",hasPermission(PERMISSIONS.ADMIN_MANAGE_ROLES),async(req,res)=>{try{const role=await Role.findOne({name:req.params.name});if(!role)return res.status(404).json({error:"Not found"});res.json(role)}catch(err){res.status(500).json({error:err.message})}});router.post("/",hasPermission(PERMISSIONS.ADMIN_MANAGE_ROLES),async(req,res)=>{try{const{name,permissions}=req.body;const role=await Role.create({name,permissions});res.status(201).json(role)}catch(err){res.status(400).json({error:err.message})}});router.put("/:name",hasPermission(PERMISSIONS.ADMIN_MANAGE_ROLES),async(req,res)=>{try{const{permissions}=req.body;const role=await Role.findOneAndUpdate({name:req.params.name},{permissions,updatedAt:new Date()},{new:true});if(!role)return res.status(404).json({error:"Not found"});res.json(role)}catch(err){res.status(400).json({error:err.message})}});router.delete("/:name",hasPermission(PERMISSIONS.ADMIN_MANAGE_ROLES),async(req,res)=>{try{const role=await Role.findOneAndDelete({name:req.params.name});if(!role)return res.status(404).json({error:"Not found"});res.json({message:"Deleted"})}catch(err){res.status(500).json({error:err.message})}});router.post("/check",async(req,res)=>{try{const{role,permission}=req.body;const{ROLES}=require("./rbac");const rc=ROLES[role];if(!rc)return res.status(400).json({error:"Invalid role"});res.json({role,permission,granted:rc.permissions.includes(permission)})}catch(err){res.status(500).json({error:err.message})}});module.exports=router;