[1mdiff --cc server.js[m
[1mindex 350d53b73,e5f653d2d..000000000[m
[1m--- a/server.js[m
[1m+++ b/server.js[m
[36m@@@ -16,103 -185,12 +185,111 @@@[m [mmongoose.connect(process.env.MONGODB_UR[m
    .then(() => console.log('✅ Connected to MongoDB'))[m
    .catch(err => console.error('❌ MongoDB connection error:', err));[m
  [m
[32m++<<<<<<< HEAD[m
[32m +// ---------- API ROUTES (TUTTE prima del frontend SPA) ----------[m
[32m +app.post('/buy-myz', (req, res) => {[m
[32m +  const { userTariWallet, amountMYZ } = req.body;[m
[32m +  const order = createOrder(userTariWallet, amountMYZ);[m
[32m +  onPaymentReceived(order.id, 10);[m
[32m +  res.json({ orderId: order.id, xmrAddress: order.xmrAddress, amountXMR: order.amountXMR, status: 'pending' });[m
[32m +});[m
[32m +[m
[32m +app.post('/escrow/create', (req, res) => {[m
[32m +  const { escrowId, buyer, seller, amount } = req.body;[m
[32m +  try {[m
[32m +    const id = createEscrow(escrowId, buyer, seller, amount);[m
[32m +    res.json({ escrowId: id, status: 'created' });[m
[32m +  } catch (err) {[m
[32m +    res.status(400).json({ error: err.message });[m
[32m +  }[m
[32m +});[m
[32m +[m
[32m +app.use('/api/rewards', require('./routes/rewards'));[m
[32m +app.use('/api/bounty', require('./routes/bounty'));[m
[32m +app.use('/api/stake', require('./routes/stake'));[m
[32m +app.use('/api/escrow/house', require('./routes/escrowHouse'));[m
[32m +[m
[32m +console.log('✅ Caricamento routes robot...');[m
[32m +app.use('/api/robot', require('./routes/robot'));[m
[32m +app.use('/api/robot/escrow', require('./routes/robotEscrow'));[m
[32m +app.use('/api/robot/logo', require('./routes/robotLogo'));[m
[32m +[m
[32m +console.log('✅ Caricamento routes robotCode...');[m
[32m +app.use('/api/robot/code', require('./routes/robotCode'));[m
[32m +[m
[32m +console.log('✅ Caricamento routes robotAnimal...');[m
[32m +app.use('/api/robot/animal', require('./routes/robotAnimal'));[m
[32m +[m
[32m +app.use('/api/backup', require('./routes/backup'));[m
[32m +[m
[32m +[m
[32m +// Notification status (Bounty B6)[m
[32m +app.get('/api/notifications/status', (req, res) => {[m
[32m +  const { getStatus } = require('./notifications');[m
[32m +  res.json({ success: true, data: getStatus() });[m
[32m +});[m
[32m +[m
[32m +app.get('/health', (req, res) => {[m
[32m +  res.json({[m
[32m +    status: 'healthy',[m
[32m +    timestamp: new Date().toISOString(),[m
[32m +    services: {[m
[32m +      telegram: !!process.env.TELEGRAM_BOT_TOKEN,[m
[32m +      github: !!process.env.GITHUB_TOKEN,[m
[32m +      ai: !!process.env.OPENAI_API_KEY[m
[32m +    }[m
[32m +  });[m
[32m +});[m
[32m +[m
[32m +// ---------- FRONTEND STATIC SERVING (DOPO le API) ----------[m
[32m +const frontendDist = path.join(__dirname, 'frontend', 'dist');[m
[32m +if (fs.existsSync(frontendDist)) {[m
[32m +  app.use(express.static(frontendDist));[m
[32m +  app.use((req, res, next) => {[m
[32m +    if (req.path.startsWith('/api') || req.path.startsWith('/health')) {[m
[32m +      return next();[m
[32m +    }[m
[32m +    res.sendFile(path.join(frontendDist, 'index.html'));[m
[32m +  });[m
[32m +  console.log(`✅ Serving frontend from ${frontendDist}`);[m
[32m +} else {[m
[32m +  console.log('ℹ️ Frontend dist not found. Run "npm run build" in frontend/ first.');[m
[32m +}[m
[32m +[m
[32m +// ---------- START SERVER ----------[m
[32m +const PORT = process.env.PORT || 10000;[m
[32m++=======[m
[32m++>>>>>>> origin/main[m
  const server = app.listen(PORT, () => {[m
    console.log(`🚀 Gateway running on http://localhost:${PORT}`);[m
[32m+   console.log(`🔒 Security: Rate limiting (100 req/15min), Headers active`);[m
  });[m
  [m
[32m++<<<<<<< HEAD[m
[32m +// WebSocket server (Bounty B6)[m
[32m +const WebSocket = require('ws');[m
[32m +const wss = new WebSocket.Server({ noServer: true });[m
[32m +const { addWsClient } = require('./notifications');[m
[32m +[m
[32m +server.on('upgrade', (request, socket, head) => {[m
[32m +  if (request.url === '/ws') {[m
[32m +    wss.handleUpgrade(request, socket, head, (ws) => {[m
[32m +      wss.emit('connection', ws, request);[m
[32m +    });[m
[32m +  } else {[m
[32m +    socket.destroy();[m
[32m +  }[m
[32m +});[m
[32m +[m
[32m +wss.on('connection', (ws) => {[m
[32m +  console.log('[WS] Client connected');[m
[32m +  addWsClient(ws);[m
[32m +  ws.send(JSON.stringify({ event: 'connected', timestamp: new Date().toISOString() }));[m
[32m +});[m
[32m +[m
[32m++=======[m
[32m+ // Graceful shutdown[m
[32m++>>>>>>> origin/main[m
  process.on('SIGTERM', () => {[m
    console.log('🛑 SIGTERM ricevuto, chiusura graceful...');[m
    server.close(() => {[m
